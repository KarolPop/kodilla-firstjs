var a, h, triangleArea;

a = prompt('Input the longest triange side value');
h = prompt('Input the triangle height value');;

triangleArea = a*h/2;

console.log(triangleArea);

alert('The area of the trianle is ' + triangleArea);